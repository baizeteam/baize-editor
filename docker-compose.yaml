name: baize-editor
services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    image: sulgweb/baize-editor-client:latest
    ports:
      - "6651:80"
    restart: unless-stopped

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    image: sulgweb/baize-editor-server:latest
    ports:
      - "6652:6652"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
