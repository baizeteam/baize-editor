---
description: client/src/components 目录的 SDD（spec-kit 对齐）模块结构与规格门禁
globs: client/src/components/**/*
alwaysApply: false
---

# client/src/components SDD 约定

## 模块即规格单元

- `client/src/components` 下**每个一级子目录**是一个 UI 模块（当前：`Cursors`、`Toolbar`、`TableMenu`）。
- 每个模块**必须**包含 `module.spec.md`，至少包含：`Intent`、`User-visible behavior`、`Dependencies`、`Boundaries / Non-goals`、`Public API`。
- 用户可见行为或对外导出变更时，**同 PR 内**更新对应 `module.spec.md`；目录级约定变更时更新 `specs/001-client-components/spec.md`。

## 公开 API

- 业务代码引用组件时**优先**使用 `client/src/components/index.ts`（barrel），避免 `../components/Foo` 深层路径在新功能中继续扩散。
- 新增模块：在 `index.ts` 导出公开符号，并在 `module.spec.md` 的 Public API 中写明。

## 文件组织

- 模块入口一般为 `index.tsx`；样式与辅助代码与入口同目录共置（如 `*.less`、`styles.ts`、`helper.ts`）。
- 仅模块内部使用的子组件放在同一目录，不必写入 barrel；若行为对用户可见，在 `module.spec.md` 的 Subcomponents 或 User-visible behavior 中说明。

## 与 spec-kit 的关系

- 更大范围的功能仍使用 `/speckit-specify`、`/speckit-plan` 等在 `specs/<branch>/` 下落地；本目录 `module.spec.md` 是**实现侧**与规格追溯的锚点，不替代特性级 `spec.md`。

## 与 `client/src/core`

- Slate 插件、自定义类型与 `tableConfig` 位于 `client/src/core/`，与 UI 组件解耦；`components/editor/` 仅负责组合 Yjs、工具栏与可编辑区。
